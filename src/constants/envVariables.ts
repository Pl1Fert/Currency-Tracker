const { COIN_API_KEY } = process.env;
const { CURRENCY_API_KEY } = process.env;
const { FIXER_API_KEY } = process.env;
const { FIXER_URL } = process.env;
const { COINLAYER_API_KEY } = process.env;
const { COINLAYER_URL } = process.env;

export const ENV_VARS = {
    COIN_API_KEY,
    CURRENCY_API_KEY,
    FIXER_URL,
    FIXER_API_KEY,
    COINLAYER_API_KEY,
    COINLAYER_URL,
};
